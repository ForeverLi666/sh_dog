# ShDog Agent 协作约定

本文件适用于整个仓库，记录长期稳定的工程约束与协作方式。开始实质工作前，先阅读：

- `README.md`：当前可用入口；
- `docs/handoff.md`：当前状态、下一步与验证命令；
- `docs/robot_model.md`：机器人模型和执行器事实；
- `docs/architecture.md`：模块边界与目标结构。

不要在本文件复制易变化的任务进度；进度只更新到 `docs/handoff.md`。

## 协作方式

- 把用户指令理解为目标、约束和验收意图，不把用户提出的具体实现方式机械地当成唯一答案。
- 主动检查方案是否正确、必要、简洁且符合长期架构。若用户建议会造成重复、边界混乱或维护负担，
  应明确说明原因并提出更合理的最小方案。
- 对低风险细节自行作出合理判断；只有会显著改变行为、接口、范围或安全性的选择才需要先确认。
- 不为迎合“简洁”而牺牲正确性、可验证性或清晰边界。发现已有判断不合理时，直接指出并修正。
- 沟通先给结论和关键依据，保持简短、具体、可执行；不堆砌过程、套话或无关背景。

## 简洁与代码风格

- 简洁意味着职责清楚、事实唯一、路径直接，而不是文件越少或代码越集中越好。
- 配置文件声明启用项、参数与组合关系；非平凡计算放入职责明确的实现模块。避免超大配置文件，
  也避免把紧密相关的少量逻辑过度拆散。
- 不新增没有独立价值的脚本、包装层、一次性抽象、通用插件系统或兜底分支。已有标准入口能完成验证时，
  不再建立重复的检查入口。
- 单次使用的局部变量只有在表达含义、缩短复杂表达式或便于调试时才保留；否则直接使用原表达式。
- 入口保持薄；不建立无限扩张的 `common/`、`misc/` 或 `utils/`。
- 注释解释原因、物理含义和约束，不复述代码。文档使用中文，代码、配置字段和日志使用英文。
- 优先做与当前目标直接相关的最小改动，不顺手重构无关代码，不覆盖用户已有修改。

## 工程边界

- 最终链路包含 Isaac Lab 训练、策略导出、共享 C++ runtime、MuJoCo sim2sim 和实机 sim2real；
  保持各模块依赖隔离。
- Isaac Lab 仅属于 `training/`。部署代码不得依赖训练包；sim2sim 与 sim2real 共享同一 C++ runtime，
  仅替换后端。
- 策略包是训练与部署的唯一边界。部署不读取训练 checkpoint，也不自行推断 observation、action、
  关节顺序或缩放。
- `assets/sh_dog/model.toml` 是机器人名称、关节顺序、默认姿态、RS06 参数和 collision 的可编辑
  事实来源。原始 CAD URDF 与生成的规范化 URDF 均不得手工修改。
- 所有上层接口使用关节侧位置、速度、力矩和惯量；协议、方向、单位与传动换算由 MCU/后端边界处理，
  策略 runtime 不重复换算。
- `SH_DOG_STAND_CFG` 只用于状态机站起；`SH_DOG_CFG` 只用于训练及部署运动控制，禁止混用。
- 采用上游或开源基线时，先对齐其已启用且与目标相关的部分，再保留 ShDog 的资产、执行器和接口事实；
  不复制注释掉的功能、无关地形或未经验证的复杂度。
- 不为尚未出现的需求预建框架。新增依赖、公共接口或跨模块事实前，先证明当前确有使用者。
- 根目录 `scripts/` 保存仓库级生成、同步和部署入口；`training/scripts/` 只保存 Isaac Lab 专属入口。
- 训练服务器可以接收完整工程镜像；实机同步必须采用部署文件白名单，禁止同步训练代码和训练产物。

## 修改与验证

- 开始前检查 `git status` 和相关实现，区分已有修改与本次修改；工作树非干净不是清理用户改动的理由。
- 修改事实时同步检查其唯一来源和生成链路，避免在 Python、C++、配置与文档中手工维护副本。
- 先完成静态检查、语法检查和不启动大型仿真的轻量验证。主要 Isaac Sim、训练、sim2sim 和实机命令
  默认由用户执行；除非用户明确要求，不代跑长时间或高资源任务。
- 交付时说明改了什么、关键取舍、已完成的检查，并给出可直接复制的验证命令和预期观察点。
- 用户验证通过前不提交 Git。只有用户明确通知验证通过并要求后，才给出提交范围和 commit message 建议
  或执行提交。
- 接口、命令或当前阶段发生变化时更新对应文档：长期设计写入专题文档，阶段状态写入
  `docs/handoff.md`，README 只保留常用入口。
